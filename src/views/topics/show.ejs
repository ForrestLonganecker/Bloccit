<!DOCTYPE html>
<html>
    <head>

        <% include ../static/partials/head.ejs %>

    </head>
    <body>

        <% include ../static/partials/navbar.ejs %>

        <main class="container">

            <a href="/topics/<%= topic.id %>/flairs/new" class="btn btn-success">Add Flair</a>

            <h1><%= topic.title %></h1>

            <div class="row">
                <% topic.flairs.forEach((flair) => { %>
                    <a href="/topics/<%= flair.topicId %>/flairs/<%= flair.id %>" class="col" style="background-color:<%= flair.color%>;"> <%= flair.name %> </a>
                <% }) %>
            </div>

            <p><%= topic.description %></p>

            <a href="/topics/<%= topic.id %>/edit" class="btn btn-warning">Edit</a>
            <a href="/topics/<%= topic.id %>/posts/new" class="btn btn-success">New Post</a>

            <form style="display:initial" action="/topics/<%= topic.id %>/destroy" method="post">
                <button type="submit" class="btn btn-danger">Delete</button>
            </form>

            <hr>

            <h1>Posts</h1>
            <ul class="list-group">
                <% topic.posts.forEach((post) => { %>
                    <li class="list-group-item">
                        <a href="/topics/<%= topic.id %>/posts/<%= post.id %>"> <%= post.title %> </a>
                    </li>
                <% }) %>
            </ul>
        </main>

        <% include ../static/partials/baseScripts.ejs %>

    </body>
</html>